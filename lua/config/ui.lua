-- lua/config/ui.lua
-- UI configuration shared across keymaps and other config modules.
-- float_border: border style for floating terminal windows.
-- Valid values: "none" | "single" | "double" | "rounded" | "solid" | "shadow"
return {
  float_border = "none",
}
